{
  "name": "greatflorida",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run compile:sass & npm run compile:pug & npm run browser-sync",
    "compile:sass": "sass --watch scss:assets/css",
    "compile:pug": "pug ./pug/*.pug --watch --pretty --out ./",
    "browser-sync": "browser-sync start --server --files 'scss/*.scss, *.html, assets/css/*.css'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "pug-cli": "^1.0.0-alpha6",
    "sass": "^1.85.1"
  },
  "dependencies": {
    "autoprefixer": "^10.4.21",
    "bootstrap": "^5.3.3",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.0",
    "sass-rem": "^4.0.0"
  }
}
