mixin fullBannerText(image, text, variant='default')
  div(class=`full-banner__section py-3 text-white ${variant === 'blue' ? 'bg-info' : 'bg-primary'}`)
    .container
      .image-with-overlay
        img(src=image, alt="image description", width="1440")
      h2.h1= text